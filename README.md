# SolarkBot Photo Atelier

A cinematic Next.js experience for `nft.solarkbot.xyz` focused entirely on image generation. The app stages a 3D digital atelier where a user writes a prompt into a notebook, watches the artist "draw" through a timed studio sequence, reveals the final image fullscreen, and downloads the result.

NFT minting is intentionally disabled for now. The runtime, UI, routes, and docs have been reduced to a generation-only product surface.

## Stack

- Next.js 16 App Router with TypeScript
- Tailwind CSS v4
- React Three Fiber + drei
- Framer Motion
- Zustand
- NVIDIA hosted image generation API
- Vitest + Testing Library + Playwright

## Local Setup

1. Install dependencies:

```bash
npm install
```

2. Copy `.env.example` to `.env.local` and fill in the required secrets.

3. Start the dev server:

```bash
npm run dev
```

4. Open [http://localhost:3000](http://localhost:3000).

## Environment

Required server variables:

- `NVIDIA_API_KEY`
- `NVIDIA_IMAGE_MODEL`
- `NVIDIA_API_BASE_URL`

Required public variables:

- `NEXT_PUBLIC_APP_URL`

## How Image Generation Works

1. `POST /api/generate-image` validates the prompt, negative prompt, and aspect ratio.
2. The route calls `lib/nvidia/generateImage.ts`, which wraps the NVIDIA API and maps supported aspect ratios to the official SD 3.5 Large resolutions.
3. The generated bytes are converted to an inline `data:image/...;base64,...` URL on the server.
4. The client receives a frontend-safe payload containing `imageUrl`, `artifactId`, dimensions, and prompt echoes.
5. The reveal screen lets the user download the finished image or return to the studio to create another.

If NVIDIA changes the hosted endpoint path or request schema, update `lib/nvidia/generateImage.ts`. That file remains the single integration boundary for the image provider.

## Download Flow

- The reveal stage works directly from the returned inline image data.
- The client saves it with the format `solarkbot-art-YYYYMMDD-HHmmss.png`.

## NFT Status

- NFT minting is currently disabled.
- The prior Solana wallet, mint route, and metadata-preparation flow have been removed from the active app surface.
- If minting is reintroduced later, it should be added back as a separate pass on top of this generation-only baseline.

## Scripts

```bash
npm run dev
npm run lint
npm run build
npm test
npm run test:e2e
```

## Known Assumptions

- The artist is a stylized primitive stand-in rather than a rigged character, with camera choreography and prop motion carrying the performance.
- Generated images are returned inline and are not persisted between sessions.
- The current product goal is downloadable image generation only, not on-chain minting.
